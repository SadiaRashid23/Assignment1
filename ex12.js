"use strict";
/* Ex: 12 Greetings: Start with the array you used in Exercise 11, but instead of just printing
each person’s name, print a message to them. The text of each message should be the same, but each
 message should be personalized with the person’s name.*/
let p_names = ["Emma", "Martha", "Roseline"];
let msg = "hope you are doing well.";
console.log(`Hello ${p_names[0]} ! ${msg}`);
console.log(`Hello ${p_names[1]} ! ${msg}`);
console.log(`Hello ${p_names[2]} ! ${msg}`);
